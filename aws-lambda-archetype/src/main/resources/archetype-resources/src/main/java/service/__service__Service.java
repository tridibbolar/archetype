package ${groupId}.service;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class ${service}Service{
	static final Logger LOG = LogManager.getLogger(${service}Service.class);
	
	//TODO
}